function enter(pi) {
            if (pi.getQuestStatus(3856) == 0) {
                pi.sendOk("쾌스트가 진행중이 아닙니다.");
                pi.dispose();
                return;
}
        pi.warp(252020000,1);
    return true;
}